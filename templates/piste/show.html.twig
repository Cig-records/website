{% extends 'base.html.twig' %}

{% block title %}Piste{% endblock %}

{% block body %}
    <h1>Piste</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ piste.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ piste.title }}</td>
            </tr>
            <tr>
                <th>Path</th>
                <td>{{ piste.path }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('piste_index') }}">back to list</a>

    <a href="{{ path('piste_edit', {'id': piste.id}) }}">edit</a>

    {{ include('piste/_delete_form.html.twig') }}
{% endblock %}
